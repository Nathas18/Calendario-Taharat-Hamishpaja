
// Administración privada de Luaj Tahará. UI solamente; no modifica cálculos halájicos.
(function luajAdminPanel(){
 const ADMIN_EMAIL='admcalendarioth@gmail.com',FALLBACK='Para Berajá, Hatzlajá y Zera Shel Kayama de todo Am Israel';
 async function globalDedication(){try{const j=await apiCall('load_global_settings');const el=document.querySelector('.luajDedication');if(el&&j?.dedication)el.textContent=j.dedication}catch(e){console.warn('Mensaje global',e)}}
 async function init(){
  globalDedication();const s=await validSession();if(String(s?.user?.email||'').toLowerCase()!==ADMIN_EMAIL)return;
  let tries=0;while(!document.getElementById('settingsModal')&&tries++<30)await new Promise(r=>setTimeout(r,100));
  const modal=document.getElementById('settingsModal');if(!modal)return;const tabs=modal.querySelector('.settingsTabs'),content=modal.querySelector('.settingsContent');if(!tabs||!content||modal.querySelector('[data-tab="admin"]'))return;
  const tab=document.createElement('button');tab.type='button';tab.className='settingsTab';tab.dataset.tab='admin';tab.textContent='Administración';tabs.appendChild(tab);
  const pane=document.createElement('div');pane.className='settingsPane';pane.dataset.pane='admin';pane.innerHTML='<div class="settingsPaneIntro"><b>Administración de Luaj Tahará</b><span>Herramientas privadas del administrador.</span></div><div class="settingsSlot"><div class="card settingsMovedCard"><b>Usuarios registrados</b><div id="adminUserCount" style="font-size:32px;font-weight:800;margin-top:8px">—</div><div class="notice">Total de cuentas registradas en Luaj Tahará.</div></div><div class="card settingsMovedCard" style="margin-top:12px"><b>Mensaje global</b><div class="notice" style="margin:8px 0">Este texto aparece debajo de “Luaj Tahará” para todos los usuarios.</div><textarea id="adminDedication" maxlength="240" rows="4" style="width:100%;box-sizing:border-box;border:1px solid #d8e5e3;border-radius:12px;padding:12px;font:inherit;resize:vertical" placeholder="Ej. Mes donado por Familia Cohen"></textarea><button type="button" class="btn" id="adminSaveDedication" style="margin-top:10px">Guardar para todos</button><div id="adminDedicationMsg" class="notice" style="margin-top:8px"></div></div></div>';content.appendChild(pane);
  async function loadAdmin(){try{const[a,g]=await Promise.all([apiCall('admin_stats',{},s.access_token),apiCall('admin_load_global_settings',{},s.access_token)]);document.getElementById('adminUserCount').textContent=String(a.total_users??0);document.getElementById('adminDedication').value=g.dedication||FALLBACK}catch(e){document.getElementById('adminDedicationMsg').textContent='No se pudo cargar: '+e.message}}
  tab.onclick=()=>{modal.querySelectorAll('.settingsTab').forEach(x=>x.classList.toggle('active',x.dataset.tab==='admin'));modal.querySelectorAll('.settingsPane').forEach(x=>x.classList.toggle('active',x.dataset.pane==='admin'));content.scrollTop=0;loadAdmin()};
  document.getElementById('adminSaveDedication').onclick=async()=>{const input=document.getElementById('adminDedication'),msg=document.getElementById('adminDedicationMsg'),btn=document.getElementById('adminSaveDedication'),dedication=input.value.trim();if(!dedication){msg.textContent='Escribe un mensaje antes de guardar.';return}btn.disabled=true;msg.textContent='Guardando…';try{const j=await apiCall('admin_save_global_settings',{dedication},s.access_token);document.querySelectorAll('.luajDedication').forEach(x=>x.textContent=j.dedication);msg.textContent='Guardado. El cambio se mostrará a todos los usuarios.'}catch(e){msg.textContent='No se pudo guardar: '+e.message}finally{btn.disabled=false}};
 }
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();