
(function kavuaPersistenceAndJoserBeneor(){
 const basePossible=possiblePatterns,baseAuto=autoEvents,baseRenderPatterns=renderPatterns;
 function reiyot(){return state.events.filter(e=>e.type==='reiya').sort((a,b)=>a.date.localeCompare(b.date)||a.ona.localeCompare(b.ona))}
 function establishmentHistory(){const re=reiyot(),found=[];for(let i=2;i<re.length;i++){const x=re.slice(i-2,i+1),h=x.map(r=>hebParts(atNoon(r.date)));if(h.every(Boolean)&&sameOna(x)&&consecutiveHebMonths(x)&&h[0].day===h[1].day&&h[1].day===h[2].day)found.push({type:'jodesh-kavua',kind:'jodesh',ona:x[2].ona,targetDay:h[2].day,establishedAt:x[2].date});const wd=x.map(r=>atNoon(r.date).getDay()),ints=[1+dayDiff(x[0].date,x[1].date),1+dayDiff(x[1].date,x[2].date)];if(sameOna(x)&&wd[0]===wd[1]&&wd[1]===wd[2]&&ints[0]===ints[1]&&[8,15,22,29].includes(ints[0]))found.push({type:'hashavua',kind:'hashavua',ona:x[2].ona,weekday:wd[2],interval:ints[0],establishedAt:x[2].date})}for(let i=3;i<re.length;i++){const x=re.slice(i-3,i+1),ints=[1+dayDiff(x[0].date,x[1].date),1+dayDiff(x[1].date,x[2].date),1+dayDiff(x[2].date,x[3].date)];if(sameOna(x)&&ints[0]===ints[1]&&ints[1]===ints[2])found.push({type:'haflaga-kavua',kind:'haflaga',ona:x[3].ona,interval:ints[2],establishedAt:x[3].date})}return found.sort((a,b)=>a.establishedAt.localeCompare(b.establishedAt))}
 function signature(p){return p.kind+'|'+p.ona+'|'+(p.targetDay||'')+'|'+(p.weekday??'')+'|'+(p.interval||'')}
 function nextProjection(p,anchor){if(p.kind==='jodesh')return immediateNextHebMonthTarget(anchor,p.targetDay);const d=atNoon(anchor);d.setDate(d.getDate()+p.interval-1);return localISO(d)}
 function confirmations(p){return state.events.filter(e=>isValidKavuaAbsence(e)&&e.patternType===p.type&&e.ona===p.ona&&e.date>p.establishedAt).sort((a,b)=>a.date.localeCompare(b.date))}
 function reiyaAt(date,ona){return reiyot().find(r=>r.date===date&&r.ona===ona)}
 function resolvedNoWithoutAbsence(p,date){return state.events.find(e=>isResolvedKavuaNoWithoutAbsence(e)&&e.patternType===p.type&&e.ona===p.ona&&e.date===date)}
 function lifecycleFor(p,all){const replacement=all.find(q=>q.establishedAt>p.establishedAt&&signature(q)!==signature(p));if(replacement)return {...p,status:'replaced',replacement};let anchor=p.establishedAt,misses=0,lastNo=null;for(let guard=0;guard<60;guard++){const expected=nextProjection(p,anchor);if(!expected)break;const yes=reiyaAt(expected,p.ona);if(yes){anchor=yes.date;misses=0;continue}const no=confirmations(p).find(n=>n.date===expected);if(no){misses++;anchor=no.date;lastNo=no;if(misses>=3)break;continue}const noBedika=resolvedNoWithoutAbsence(p,expected);if(noBedika){anchor=noBedika.date;continue}return {...p,status:'active',misses,projection:expected,pending:true,lastNo}}
 if(misses<3)return {...p,status:'active',misses,projection:nextProjection(p,anchor),pending:true,lastNo};const dormantFrom=lastNo.date,later=reiyot().filter(r=>r.date>dormantFrom);let wake=null;if(p.kind==='jodesh')wake=later.find(r=>{const h=hebParts(atNoon(r.date));return r.ona===p.ona&&h&&h.day===p.targetDay})||null;else if(p.kind==='hashavua')wake=later.find(r=>r.ona===p.ona&&atNoon(r.date).getDay()===p.weekday)||null;else{for(let i=1;i<later.length;i++){const a=later[i-1],b=later[i];if(b.ona===p.ona&&1+dayDiff(a.date,b.date)===p.interval){wake=b;break}}}if(wake)return {...p,status:'active',misses:0,joserBeneor:true,wakeDate:wake.date,projection:nextProjection(p,wake.date),pending:true};return {...p,status:'dormant',misses:3,dormantFrom}}
 function lifecycle(){const hist=establishmentHistory();if(!hist.length)return {hist,latest:null};const states=hist.map(p=>lifecycleFor(p,hist)),latest=states.slice().reverse().find(s=>s.status!=='replaced')||null;return {hist,states,latest}}
 function toPattern(s){if(!s||s.status!=='active'||!s.projection)return null;let title='Posible Veset Kavúa',detail='Kavúa vigente.';if(s.kind==='jodesh'){title='Posible Veset HaJódesh Kavúa';detail='Veset HaJódesh fijo del día '+s.targetDay+', en la misma oná.'}if(s.kind==='hashavua'){title='Posible Veset HaShavúa Kavúa';detail='Veset HaShavúa vigente con intervalo inclusivo fijo de '+s.interval+' días, en la misma oná.'}if(s.kind==='haflaga'){title='Posible Veset Haflagá Kavúa';detail='Veset Haflagá vigente de '+s.interval+' días, en la misma oná.'}if(s.misses)detail+=' Lleva '+s.misses+' ausencia'+(s.misses===1?'':'s')+' confirmada'+(s.misses===1?'':'s')+'. El Kavúa sigue vigente hasta tres ausencias confirmadas.';if(s.joserBeneor)detail+=' Reactivado por Joser Beneor.';detail+=' Al llegar esta oná se debe confirmar expresamente si hubo Reiyá: el sistema nunca supone un NO.';return {...s,type:s.type,title,detail,ona:s.ona,projection:s.projection,kavuaLifecycle:true}}
 activeKavuaPatterns=function(){const s=lifecycle().latest,p=toPattern(s);return p?[p]:[]};
 autoEvents=function(){const life=lifecycle(),s=life.latest,p=toPattern(s);if(!p)return baseAuto();let ordinary=[];if(s.misses>0){const saved=activeKavuaPatterns;try{activeKavuaPatterns=()=>[];ordinary=baseAuto()}finally{activeKavuaPatterns=saved}ordinary=ordinary.filter(e=>e.type!=='beinoni'&&!(e.orSource==='beinoni'));}const no=state.events.find(e=>isValidKavuaAbsence(e)&&e.date===p.projection&&e.ona===p.ona&&e.patternType===p.type);ordinary.push({date:p.projection,ona:p.ona,type:'pattern',auto:true,label:no?'Kavúa · NO confirmado':'Kavúa · confirmar Reiyá',pattern:p,confirmedNo:!!no});return ordinary};
 possiblePatterns=function(){const base=basePossible(),s=lifecycle().latest,p=toPattern(s),fixed=['jodesh-kavua','hashavua','haflaga-kavua'];let out=base.filter(x=>!fixed.includes(x.type));if(p)out.push(p);else if(s&&s.status==='dormant')out.push({type:s.type,title:'Veset Kavúa dormido · Joser Beneor',detail:'Este último Kavúa fue arrancado después de tres ausencias confirmadas y no fue reemplazado. La mujer vuelve al régimen ordinario de Oná Beinonit, Yom HaJódesh y Haflagá. El Kavúa queda dormido para Joser Beneor; si posteriormente se establece otro Kavúa, éste lo reemplaza y el anterior deja de poder regresar.',projection:null,ona:s.ona,kavuaDormant:true});return out};
 renderPatterns=function(){baseRenderPatterns()};setTimeout(()=>render(),250);authListeners.push(()=>setTimeout(()=>render(),450));
})();
